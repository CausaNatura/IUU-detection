import { FrontendConfig } from 'types/frontConfig';

export const defaultConfig: FrontendConfig = {
  global: {
    logo: 'default',
    name: 'Cuestionarios Designa',
    theme: 'default',
    latex: 'LATEX_',
    socketPort: 3000,
    socketUrl: 'http://localhost',
  },
};
